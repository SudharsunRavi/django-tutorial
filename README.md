Use virtual env

Verify version --> django-admin --version
Create foler --> django-admin startproject projectName

Run server--> python manage.py runserver

To create an app-->python manage.py startapp appName

Add appName to INSTALLED_APPS in settings.py in projectName